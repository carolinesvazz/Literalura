spring.application.name=literalura

# Configuração para o banco de dados PostgreSQL
# Certifique-se de que o banco de dados 'literalura' exista.
spring.datasource.url=jdbc:postgresql://localhost:5432/literalura
spring.datasource.username=postgres
spring.datasource.password=276419

# Permite que o Hibernate atualize o banco de dados com base nas suas entidades
spring.jpa.hibernate.ddl-auto=update